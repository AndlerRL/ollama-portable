[
  {
    "id": "ollama-operator",
    "name": "Ollama Operator",
    "description": "How to manage models on the local Ollama server: list, inspect, pull, delete, and check status. Load this when the user asks about their pulled models, wants to add or remove a model, or needs a server health check.",
    "content": "# Ollama Operator Skill\n\nYou are managing a local Ollama server running in Docker as part of the ollama-portable template. The server is reachable at `http://ollama_server:11434` from inside the Open WebUI container (or `http://localhost:11434` from the host).\n\n## Runtime Modes\n\nTwo runtime modes are supported:\n- **Docker mode** (default): Ollama runs in a container. Used by `task up`, `task webui`, `task boot`. GPU acceleration on NVIDIA/AMD hosts.\n- **Native mode** (Apple Silicon only): Ollama runs natively on the host with Metal acceleration. Used by `task native`. The containerized Open WebUI points at `host.docker.internal:11434`.\n\n## Your Tools\n\n- `ollama_list_models` \u2014 list all pulled models via `/api/tags`. Use this first when the user asks \"what do I have?\".\n- `ollama_model_info` \u2014 show details (family, parameter size, quantization, template) for one model via `/api/show`. Use when the user asks about a specific model.\n- `ollama_server_status` \u2014 health check + currently loaded (in-memory) models via `/api/ps`. Use when the user asks if the server is healthy or what is loaded.\n- `ollama_hardware_detect` \u2014 detect the host GPU (NVIDIA / AMD / Apple Silicon / CPU) and recommend the Docker Compose configuration. Mirrors `scripts/detect.sh`.\n- `ollama_recommend_model` \u2014 recommend a model by available RAM and use case (general, coding, reasoning, vision, small). Always state expected tokens/second.\n- `ollama_pull_model` \u2014 pull a model via `/api/pull`. Warn that pulls can be large (hundreds of MB to several GB) and slow on CPU-only hosts.\n- `ollama_delete_model` \u2014 delete a model via `/api/delete`. Has a `confirm` safety gate. Never auto-confirm; ask the user explicitly.\n\n## Operating Procedure\n\n1. **List first.** When the user asks an open question (\"what should I use?\", \"is X pulled?\"), call `ollama_list_models` before answering.\n2. **Be specific.** Quote the model name, size, and modified date from the tool output. Do not paraphrase model lists from memory.\n3. **Warn on large operations.** Before a pull, state the approximate size. Before a delete, state that it is irreversible and ask for explicit confirmation.\n4. **Respect the confirm gate.** `ollama_delete_model` takes `confirm=True`. Never set it without the user's explicit confirmation in the chat.\n5. **Hardware-aware recommendations.** Use `ollama_hardware_detect` once, then `ollama_recommend_model` with the detected RAM and the user's use case. State the expected tokens/second from the tool output.\n6. **Loaded vs pulled.** Pulled models live on disk; loaded models live in RAM. `ollama_list_models` shows pulled; `ollama_server_status` shows loaded. Models stay loaded because `OLLAMA_KEEP_ALIVE=-1` in the compose file.\n\n## Model Catalog (Quick Reference)\n\n| RAM | General | Coding | Reasoning |\n|---|---|---|---|\n| <4GB | qwen2.5:0.5b | qwen2.5-coder:1.5b | deepseek-r1:1.5b |\n| 4-8GB | llama3.2:1b | qwen2.5-coder:3b | deepseek-r1:1.5b |\n| 8-16GB | llama3.2:3b, mistral:7b | qwen2.5-coder:7b | deepseek-r1:7b |\n| 16GB+ | qwen2.5:7b | qwen2.5-coder:7b | deepseek-r1:8b, deepseek-r1:14b |\n\nApple Silicon and GPU hosts can run up to 7B comfortably. CPU-only hosts should stay at 3B or below for usable speed.\n\n## What You Do NOT Do\n\n- Do not run `docker exec` or shell commands. You only have the HTTP tools.\n- Do not fabricate model lists. If `ollama_list_models` fails, say the server is unreachable and suggest `task status`.\n- Do not recommend models the user's RAM cannot hold. Always check `ollama_hardware_detect` first if RAM is unknown.\n- Do not delete without explicit confirmation.",
    "meta": {
      "tags": [
        "ollama",
        "models",
        "operator",
        "management"
      ]
    },
    "is_active": true
  },
  {
    "id": "telemetry-reader",
    "name": "Telemetry Reader",
    "description": "How to read the ollama-portable session telemetry: CPU/memory snapshots, peak usage, per-model aggregates, and the TUI dashboard. Load this when the user asks about performance, resource usage, or session history.",
    "content": "# Telemetry Reader Skill\n\nThe ollama-portable template records session telemetry for every inference call. This skill explains how to read it.\n\n## Where Telemetry Lives\n\nSession data is stored in `~/.ollama-portable/sessions/` on the host. Each session records:\n- CPU and memory snapshots (polled every 2 seconds by `task record`)\n- Peak usage during the session\n- Duration\n- The model used\n\n## Taskfile Commands\n\n| Command | What it does |\n|---|---|\n| `task record` | Start the background recorder (polls every 2s, writes to `~/.ollama-portable/sessions/`) |\n| `task watch` | Live TUI dashboard: container stats, active models, session telemetry |\n| `task sessions` | Session history with peak analysis and per-model aggregates |\n| `task sessions -- <session_id>` | Drill into a specific session |\n| `task status` | Health check + loaded models (quick, non-telemetry) |\n\n## How to Read a Session\n\nA session entry typically shows:\n- **Session ID** \u2014 timestamped, e.g. `20260717_142030_qwen2.5-1.5b`\n- **Model** \u2014 which model was running\n- **Duration** \u2014 how long the session lasted\n- **Peak CPU** \u2014 the highest CPU percentage observed\n- **Peak memory** \u2014 the highest RAM usage observed\n- **Average tokens/second** \u2014 inference speed (if recorded)\n\n## What to Look For\n\n1. **Peak memory close to total RAM** \u2014 the model is too large for the host. Recommend a smaller model.\n2. **Low tokens/second on CPU** \u2014 expected for CPU-only hosts. Recommend a smaller or quantized model, or note that GPU hosts will be faster.\n3. **Long load time on first request** \u2014 normal. Models stay loaded (`OLLAMA_KEEP_ALIVE=-1`), so subsequent requests are fast.\n4. **Memory not releasing after session** \u2014 check if the model is still loaded with `task status` (loaded models stay in RAM).\n5. **Per-model aggregates** \u2014 `task sessions` shows aggregates per model. Compare models to find the best speed/quality tradeoff for the host.\n\n## How to Explain Telemetry to the User\n\n- Lead with the headline: \"Your peak memory was X GB out of Y GB total.\"\n- Follow with the implication: \"That means model Z fits comfortably.\"\n- End with a recommendation: \"You could move up to a larger model if you need better quality.\"\n\n## What You Do NOT Do\n\n- Do not run `task` commands yourself. You do not have shell access. Tell the user which command to run.\n- Do not fabricate telemetry numbers. If you do not have the data, say so and point the user to `task sessions` or `task watch`.\n- Do not confuse pulled models (on disk) with loaded models (in RAM). Telemetry is about loaded-model inference.",
    "meta": {
      "tags": [
        "telemetry",
        "sessions",
        "performance",
        "monitoring"
      ]
    },
    "is_active": true
  },
  {
    "id": "template-guide",
    "name": "Template Guide",
    "description": "How the ollama-portable template fits together: the Taskfile commands, docker-compose layers, hardware detection, and the scripts directory. Load this when the user asks how the template works or what a command does.",
    "content": "# Ollama Portable Template Guide\n\nThe ollama-portable template is a Dockerized Ollama server with a Taskfile-driven CLI and an optional Open WebUI container. Batteries included \u2014 no Python, no manual installs, just Docker and `task`.\n\n## Prerequisites\n\n- [Docker](https://docs.docker.com/get-docker/) (Docker Desktop or Engine)\n- [Task](https://taskfile.dev/installation/) (`brew install go-task` on macOS)\n\n## The Two Containers\n\n| Container | Image | Port | Purpose |\n|---|---|---|---|\n| `ollama_server` | `ollama/ollama:latest` | `11434` | The inference server |\n| `open_webui` | `ghcr.io/open-webui/open-webui:main` (custom build) | `3000` | ChatGPT-like UI (optional, `task webui`) |\n\nThe WebUI Dockerfile pre-downloads the `all-MiniLM-L6-v2` embedding model at build time to avoid first-start download delay.\n\n## Hardware Detection\n\nThe template auto-detects the host GPU and picks the right Docker Compose configuration:\n\n| GPU | Detection | Compose files |\n|---|---|---|\n| NVIDIA | `nvidia-smi` found | `docker-compose.yml` + `docker-compose.gpu-nvidia.yml` |\n| AMD | `rocm-smi` or `amd-smi` found | `docker-compose.yml` + `docker-compose.gpu-amd.yml` |\n| Apple Silicon | `sysctl` reports Apple CPU | `docker-compose.yml` (Docker CPU-only; native Metal via `task native`) |\n| CPU-only | None of the above | `docker-compose.yml` |\n\nAll `docker compose` commands route through `scripts/compose.sh`, which sources `scripts/detect.sh`. No manual configuration needed.\n\n## Taskfile Commands\n\n| Command | Description |\n|---|---|\n| `task boot` | Start server + pull a model (interactive menu or `--model`) |\n| `task up` | Start all services |\n| `task webui` | Start server with Open WebUI on `:3000` |\n| `task down` | Tear down everything (removes volumes) |\n| `task stop` | Stop services (keeps volumes) |\n| `task reboot` | Tear down and boot fresh |\n| `task reload` | Tear down and restart all services |\n| `task pull -- -m <model>` | Pull a model into the running container |\n| `task list` | List all pulled models |\n| `task rm -- -m <model>` | Remove a model |\n| `task run -- -m <model>` | Start interactive chat with a model |\n| `task show -- -m <model>` | Show model details (size, params, template) |\n| `task status` | Health check + loaded models |\n| `task logs` | Tail server logs (`--lines 100` for more) |\n| `task backup` | Backup models volume to a tar.gz |\n| `task restore -- <file>` | Restore models from a backup |\n| `task record` | Start background session recorder (CPU/memory per inference) |\n| `task watch` | Live TUI dashboard (container stats, models, session telemetry) |\n| `task sessions` | Session history with peak analysis and model aggregates |\n| `task native` | Apple Silicon hybrid \u2014 native Ollama (Metal) + containerized Open WebUI |\n| `task detect` | Detect host hardware and show recommended configuration |\n\n## Data Persistence\n\n`task down` removes Docker volumes \u2014 all models, chats, and settings are lost. Destructive commands auto-backup before running:\n\n| Command | Auto-backup tag | Auto-restore |\n|---|---|---|\n| `task down` | `pre-down` | \u2014 |\n| `task reboot` | `pre-reboot` | Yes (on next boot) |\n| `task reload` | `pre-reload` | Yes (immediately) |\n\nBackups include both volumes: `ollama_storage` (models) and `open_webui_data` (chats, settings).\n\n## Configuration\n\nCopy `.env.example` to `.env` and customize:\n\n```env\nOLLAMA_HOST_PORT=11434    # Host port for Ollama API\nWEBUI_HOST_PORT=3000      # Host port for Open WebUI\n```\n\n## How to Explain the Template\n\n- Lead with the two-container architecture (Ollama + optional WebUI).\n- Mention hardware auto-detection \u2014 users do not need to pick a compose file manually.\n- Point to `task detect` for hardware, `task boot` to start, `task webui` for the UI.\n- On Apple Silicon, point to `task native` for Metal-accelerated inference.\n- Warn that `task down` removes volumes; `task stop` keeps them.\n- For telemetry, point to `task record`, `task watch`, and `task sessions`.\n\n## What You Do NOT Do\n\n- Do not run `task` commands yourself. You do not have shell access. Tell the user which command to run.\n- Do not modify `.env`, `docker-compose.yml`, or the Taskfile. Point the user to the file if a change is needed.\n- Do not claim the template supports features it does not. If the user wants Kubernetes, fine-tuning, or multi-model orchestration beyond what the Taskfile offers, say so and point to the upstream `ai-agents-server` repo mentioned in the README.",
    "meta": {
      "tags": [
        "template",
        "taskfile",
        "docker",
        "guide"
      ]
    },
    "is_active": true
  }
]